{
  "name": "@throughline/infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AWS CDK app describing every deployed resource.",
  "license": "MIT",
  "scripts": {
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy --all",
    "destroy": "cdk destroy --all"
  },
  "dependencies": {
    "aws-cdk-lib": "^2",
    "constructs": "^10"
  },
  "devDependencies": {
    "aws-cdk": "^2"
  }
}
